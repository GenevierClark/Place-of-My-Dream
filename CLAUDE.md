\# 프로젝트: 꿈속 기억 탐색 JRPG (C++ 콘솔)



\## 프로젝트 개요

\- 장르: JRPG 스타일 턴제 어드벤처 (그래프 기반 노드 탐색형, 고정 맵 구조)

\- 개발 환경: C++, Windows Console API (단, Console API는 후반부에 적용 예정)

\- 개발 기간: 30일 여름 집중 포트폴리오 프로젝트

\- 목표: C++ 클라이언트 프로그래머로서 OOP, 자료구조(그래프), 상태 관리(State Machine), 캡슐화/옵저버 패턴 역량 증명. 1차 목표는 게임인재원 9기(2027년 초 입학 예정) 합격 — 당분간 UE5 학습보다 C++ 기본기 집중. 자가진단 약점: vtable/vptr(다중 상속 포함), 템플릿+포인터/참조자 실무 활용 — 진짜 필요한 지점에서만 자연스럽게 다루고 억지로 기능에 끼워넣지 말 것 (2026-08-03 합의)



\## 핵심 게임 루프

\- 수면 심도(Sleep Depth)는 0~100 텐션 게이지 (시작값 50). 0 도달 시 층 입구서 재시작(해당 층 아이템 전부 삭제), 100 도달 시 적 전역 강화 하드모드 돌입. HP 0(전투 불능) 시엔 마지막 휴식처에서 부활(층 입구 아님, 수면 심도 소량 감소)

\- 노드 종류: 휴식처, 전투방, 엘리트 전투방, 상인의 방, 이벤트 방, 잠긴 문

\- 5개 Map 클리어가 승리 조건. 몬스터는 1순위(조건부 인터럽트)/2순위(고정 사이클) 우선순위 AI. 전투는 기본 공격(무속성)/특수 스킬(속성, 심도 변동 코스트)/방어/도구 4커맨드. 감정 속성(자각→몽환→망각→자각 순환 상성, 약점 2배/역상성 0.5배, 최소데미지 1) + 과수면(Hypersomnia, 하드모드 전용 4번째 속성, 특수 능력 미정)



\## 개발 진행 방식 (반드시 지킬 것)

1\. \*\*직접적인 완성 코드를 제시하지 말 것.\*\* 새 기능은 먼저 사용자가 스스로 설계(틀려도 됨)를 시도한 뒤 가져오면, 그걸 검증/보완하는 방식으로 피드백할 것. 방향성을 먼저 제시하지 말고, 사용자가 막혀서 명시적으로 요청할 때만 힌트/개념 설명을 제공할 것. 사용자가 직접 작성한 코드를 가져오면 그때 리뷰/피드백 진행. (이유: 방향을 먼저 제시하면 사용자가 스스로 설계를 고안하는 능력을 훈련할 기회를 놓치게 됨 — 2026-07-21 합의)

2\. \*\*30일 스코프 관리.\*\* 구현 난이도가 높거나 병목이 예상되는 기능은 현실적으로 축소/배제하는 방향으로 조언할 것.

3\. \*\*코드 리뷰 시 관련 C++ 개념을 구체적으로 설명할 것\*\* (예: 다형성, 스마트 포인터, 옵저버 패턴 등 해당 코드에 실제로 쓰인 개념 위주). 구현 방식이 여러 갈래로 갈리는 지점(예: 상속 vs enum class)에서는 정답을 바로 제시하지 말고, 사용자의 구현 의도를 먼저 묻고 각 방식의 장단점을 비교 설명할 것. (이유: 기초를 탄탄히 다지고 싶다는 사용자 요청 — 2026-07-22 합의)

4\. Windows Console API는 로직이 어느 정도 완성된 이후 단계에서 최소 범위로 적용 예정. 렌더링은 인터페이스로 분리해서 std::cout 기반 구현 → Console API 기반 구현으로 나중에 교체 가능하도록 설계할 것.



\## 현재 상태

\- 기획 확정: 맵 구조(고정), 노드 종류(6종), 자원 시스템(수면 심도 텐션 게이지 3분기), 플레이어 스탯, 열쇠 획득 방식, 맵 테마, 꿈조합 시스템(Base/Modifier 역할 분담 포함), 전투 커맨드 4종 및 3페이즈 턴, 몬스터 AI 우선순위 규약, 상점 시스템, 감정 속성 상성(확정) (상세는 docs/game\_design.md 참고)

\- 미정 (Map 1 범위로 스코프 한정): Map 1 몬스터(포근한 뱀 인형/거짓말을 하는 그림일기) 스탯·기믹·약점판정조건, Modifier별 고유 스킬 로직 상세, 엘리트 전투방 보상, 하드모드 적 강화 상세, HP 사망 시 수면심도 감소량, Map 1 서사 이벤트, Map 1 아이템 테이블, 휴식처 회복 방식/수치, 상점 물가

\- 기획 논의 히스토리: docs/devlog.md

\- 코드 구현 중 질문/리뷰/학습 기록: docs/dev\_journal.md (재학습용, 상세히 기록할 것)

\- IDE: Visual Studio 2026

\- 코드 진행 상황 (Day 4 종료 시점, 2026-08-03): Room(추상) + RestArea 완성. Character(TakeDamageHealth 가상함수)/Player(BaseAttack 스텁, 참조형으로 통일) 완성. Monster(추상, TakeDamageSleepDepth) + CozySnake 완성. 스킬을 별도 객체로 설계 — WeaponSkill(추상, Execute 순수가상) + FirstTypeSkill(구체, 속성 상성 반영 데미지 스킬, 버그 수정 완료) 완성. BattleArea는 구조만 있고 \*\*Attack 커맨드의 실제 데미지 계산 연결은 아직 미구현\*\* — 다음 세션 최우선 작업. StoreArea는 스텁. GameManager 등 중앙 매니저는 미착수.

\- \*\*Weapon 재설계 방향 확정, 구현은 내일부터\*\*: `SynthesisType`(조합 전체 열거) 폐기하고 `Weapon`에 `BaseType` + `Type`(속성) + `std::string Name` 필드로 표현하기로 함 (유효 조합 여부·이름은 별도 데이터 테이블로 처리). `ModifierItemType`도 같은 이유로 폐기하고 `struct ModifierItemData { Name, Attribute, FloorTheme }` + 데이터 테이블 방식으로 재설계 예정. 아직 코드 미반영 — docs/dev\_journal.md 24\~25절 참고.

